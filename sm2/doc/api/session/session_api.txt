class sm_session {
    /*
    **  Constructor
    **  If no $sess_id is passed in, then a new one is created.
    */
    function sm_session ($sess_id) {
    }

    /*
    **  get_id
    **  returns the sess_id
    */
    function get_id() {
    }

    /*
    **  set
    **  stores a variable in the session
    */
    function set ($name, $value) {
    }

    /*
    **  get
    **  returns the value stored in the session (false if it's not stored)
    */
    function get ($name) {
    }

    /*
    **  unset
    **  deletes a variable from the session
    */
    function unset ($name) {
    }

    /*
    **  isset
    **  returns true/false if the variable is/isn't stored in the session.
    */
    function isset ($name) {
    }

    /*
    **  destroy
    **  destroys all the session data and removes the session.
    */
    function destroy() {
    }
    
}
