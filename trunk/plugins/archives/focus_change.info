<?php

    $name = 'focus_change';

    $plugin_ary[$name]['name']     = 'Focus Change';
    $plugin_ary[$name]['author']   = 'Luke Ehresman';
    $plugin_ary[$name]['email']    = 'luke@squirrelmail.org';
    $plugin_ary[$name]['desc']     = '
        Uses JavaScript to change the focus on a number
        of forms.  This makes SquirrelMail easier to use,
        but still keeps us JavaScript free in the core.
    ';
    $plugin_ary[$name]['changes']  = array(
        array('version' => '1.0',
              'date'    => mktime(0,0,0,2,11,2001),
              'desc'    => 'Fixed some bugs with current stable versions'
        )
    );
?>
