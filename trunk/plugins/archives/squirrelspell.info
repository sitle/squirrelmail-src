<?php

    $name = 'squirrelspell';

    $plugin_ary[$name]['name']     = 'Squirrel Spell';
    $plugin_ary[$name]['author']   = 'Konstantin Riabitsev';
    $plugin_ary[$name]['email']    = 'graf@relhum.org';
    $plugin_ary[$name]['desc']     = '
        This is a spell checker with a JavaScript interface. It supports
	multiple languages, user dictionaries, and even encryption
	to protect the users\' privacy. Note that it may
        not work with older browsers that are not JavaScript capable.
        Requirements: Server: UNIX, ispell/aspell, Client: JavaScript-capable 
	browser; Optional: MCRYPT (for encryption).
    ';
    $plugin_ary[$name]['changes']  = array(
    	array('version' => '0.3.pre1',
	      'date'    => mktime(0,0,0,2,08,2001),
	      'desc'    => 'Added multiple language support, encryption, and a multitude of other features.'
	),
        array('version' => '0.2.1',
              'date'    => mktime(0,0,0,1,13,2001),
              'desc'    => 'Added "Soup Nazi" functionality -- denies service to browsers that are known to not display the plugin correctly'
        ),
        array('version' => '0.2pl1',
              'date'    => mktime(0,0,0,12,3,2000),
              'desc'    => 'Fixed some bugs with 0.2.'
        ),
        array('version' => '0.2',
              'date'    => mktime(0,0,0,11,30,2000),
              'desc'    => 'Adds functionality of user dictionaries. Still uses aspell/ispell as opposed to PHP\'s pspell which would be too much of a rewrite.'
        ),
        array('version' => '0.1',
              'date'    => mktime(0,0,0,8,28,2000),
              'desc'    => 'Fixes some bugs, especially with adding backslashes before quotes.'
        )
    );
?>
