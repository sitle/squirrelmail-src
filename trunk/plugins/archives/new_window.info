<?php

    $name = 'new_window';

    $plugin_ary[$name]['name']     = 'New Window';
    $plugin_ary[$name]['author']   = 'Luke Ehresman';
    $plugin_ary[$name]['email']    = 'luke@squirrelmail.org';
    $plugin_ary[$name]['desc']     = '
        Opens messages in a new window. Saves time when reading through a lot
        of messages, rather than having to go back to message list all the
        time. REQUIRES 1.0 
    ';
    $plugin_ary[$name]['changes']  = array(
        array('version' => '1.0',
              'date'    => mktime(0,0,0,1,29,2001),
              'desc'    => 'Initial release'
        )
    );
?>
