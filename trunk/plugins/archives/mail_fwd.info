<?php

    $name = 'mail_fwd';

    $plugin_ary[$name]['name']     = 'Mail Forward';
    $plugin_ary[$name]['author']   = 'Ritchie Low';
    $plugin_ary[$name]['email']    = 'rlow@xipware.com';
    $plugin_ary[$name]['desc']     = '
        Email forwarding for sendmail using .forward files.
    ';
    $plugin_ary[$name]['changes']  = array(
        array('version' => '1.0',
              'date'    => mktime(0,0,0,1,15,2001),
              'desc'    => 'Initial release'
        )
    );
?>
