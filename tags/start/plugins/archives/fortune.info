<?php

    $name = 'fortune';

    $plugin_ary[$name]['name']     = 'Fortune';
    $plugin_ary[$name]['author']   = 'Ryan Orth';
    $plugin_ary[$name]['email']    = 'snubber@seahat.com';
    $plugin_ary[$name]['desc']     = '
        Displays a fortune at the top of the message list.
    ';
    $plugin_ary[$name]['changes']  = array(
        array('version' => '2.0',
              'date'    => mktime(0,0,0,2,05,2001),
              'desc'    => 'Made some modifications, mainly the ability to turn it off from the display prefs.'
        ),
        array('version' => '1.0',
              'date'    => mktime(0,0,0,8,17,2000),
              'desc'    => 'Initial release'
        )
    );
?>
