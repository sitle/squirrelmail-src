<?php

    $name = 'plugin_name';

    $plugin_ary[$name]['name']     = 'Long Plugin Name';
    $plugin_ary[$name]['author']   = 'Author Name';
    $plugin_ary[$name]['email']    = 'email@author.com';
    $plugin_ary[$name]['desc']     = '
        Write up your description here.  Be sure to 
        escape any single quotes that might crop up
        in this string.  Here\'s an example.
    ';
    $plugin_ary[$name]['changes']  = array(
        array('version' => '1.4',
              'date'    => mktime(0,0,0,1,31,2001),
              'desc'    => 'Add a short description of changes here'
        ),
        array('version' => '1.3',
              'date'    => mktime(0,0,0,12,1,2000),
              'desc'    => 'Here are some changes that we made'
        )
    );
?>
