<?php

    $name = 'translate';

    $plugin_ary[$name]['name']     = 'Translate';
    $plugin_ary[$name]['author']   = 'Tyler Akins';
    $plugin_ary[$name]['email']    = 'tyler@boas.anthro.mnsu.edu';
    $plugin_ary[$name]['desc']     = '
        If you receive a message in another language, a box appears below the
        message that lets you translate it to other languages using an external
        program such as babelfish (or many others).  
    ';
    $plugin_ary[$name]['changes']  = array(
        array('version' => '1.2',
              'date'    => mktime(0,0,0,9,27,2000),
              'desc'    => 'Cleaned up HTML'
        ),
        array('version' => '1.1',
              'date'    => mktime(0,0,0,9,17,2000),
              'desc'    => 'Better support for languages set up in i18n, better support for bidirectional languages, added another server or two, fixed bugs.'
        )
    );
?>
