<?php

    $name = 'password_forget';

    $plugin_ary[$name]['name']     = 'Password Forget';
    $plugin_ary[$name]['author']   = 'Tyler Akins';
    $plugin_ary[$name]['email']    = 'tyler@boas.anthro.mnsu.edu';
    $plugin_ary[$name]['desc']     = '
        This plugin fixes the problem when IE and Mozilla (NS 6.0) try to get
        smart and remember username and password. This allows admins to make it
        impossible for these browsers to get "smart". This REQUIRES version
        0.6pre1 or later.  
    ';
    $plugin_ary[$name]['changes']  = array(
        array('version' => '1.2',
              'date'    => mktime(0,0,0,1,11,2001),
              'desc'    => 'Made it work with the changes to the login process in 1.0pre2 (cvs)'
        ),
        array('version' => '1.1',
              'date'    => mktime(0,0,0,12,6,2000),
              'desc'    => 'Made it work happily with the focus_change plugin.'
        )
    );
?>
