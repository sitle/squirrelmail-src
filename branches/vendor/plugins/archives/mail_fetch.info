<?php

    $name = 'mail_fetch';

    $plugin_ary[$name]['name']     = 'Mail Fetch';
    $plugin_ary[$name]['author']   = 'Joshua Pollak';
    $plugin_ary[$name]['email']    = '';
    $plugin_ary[$name]['desc']     = '
        Works in a similar fashion to fetchmail, as it can be set up to
        retrieve email from a POP server and download it to your IMAP server.  
    ';
    $plugin_ary[$name]['changes']  = array(
        array('version' => '0.3',
              'date'    => mktime(0,0,0,1,18,2001),
              'desc'    => 'Offers many improvements over 0.2.'
        )
    );
?>
