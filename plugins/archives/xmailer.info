<?php

    $name = 'xmailer';

    $plugin_ary[$name]['name']     = 'X-Mailer';
    $plugin_ary[$name]['author']   = 'Luke Ehresman';
    $plugin_ary[$name]['email']    = 'luke@squirrelmail.org';
    $plugin_ary[$name]['desc']     = '
        Displays the mail client used to send an email message by fetching
        the X-Mailer header from the message.  Displays it in the standard
        header view.
    ';
    $plugin_ary[$name]['changes']  = array(
        array('version' => '0.1',
              'date'    => mktime(0,0,0,1,15,2001),
              'desc'    => 'Initial release'
        )
    );
?>
